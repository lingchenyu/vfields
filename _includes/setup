{% capture jbcache %}
  <!--
  - Dynamically set liquid variables for working with URLs/paths
  -->
   {% if site.safe and site.url and site.url != '' %}
     {% assign BASE_PATH = site.url %}
     {% assign HOME_PATH = site.url %}
   {% else %}
     {% assign BASE_PATH = nil %}
     {% assign HOME_PATH = "/" %}
   {% endif %}

{% endcapture %}{% assign jbcache = nil %}
